import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Meridian Storefront — B2B Dynamic Pricing Marketplace",
  description:
    "A mock B2B marketplace powered by real-time ML dynamic pricing. Part of the Meridian Ecosystem demonstrating online regression, LLM governance, and asynchronous telemetry pipelines.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
